{#
 # MySui Online Judge
 # file: notifications.twig
 # author: MySui Team <mysuioj@gmail.com>
 #}
{% set selected = 'notifications' %}
{% extends 'templates/base.twig' %}
{% block icon %}fa-bell{% endblock %}
{% block title %}{{ lang('Notifications') }}{% endblock %}
{% block head_title %}{{ lang('Notifications') }}{% endblock %}



{% block title_menu %}
	{% if user.level >= 2 %}
	<span class="title_menu_item"><a href="{{ site_url('notifications/add') }}"><i class="fa fa-plus color10"></i> {{ lang('New') }}</a></span>
	{% endif %}
{% endblock %}



{% block main_content %}
{% if notifications|length == 0 %}
<p style="text-align: center;">{{ lang('Nothing yet...') }}</p>
{% endif %}
{% for notification in notifications %}
	<div class="notif" id="number{{ notification.id }}" data-id="{{ notification.id }}">
		<div class="notif_title" dir="auto">
			<span class="anchor ttl_n">{{ notification.title }}</span>
			<div class="notif_meta">
				{{ notification.time }}
				{% if user.level >= 2 %}
					<span class="anchor edt_n">{{ lang('Edit') }}</span>
					<span class="anchor del_n">{{ lang('Delete') }}</span>
				{% endif %}
			</div>
		</div>
		<div class="notif_text" dir="auto">
			{{ notification.text|raw }}
		</div>
	</div>
{% endfor %}
{% endblock %}  {# main_content #}